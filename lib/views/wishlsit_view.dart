import 'package:flutter/material.dart';

class WishlsitView extends StatelessWidget {
  const WishlsitView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(backgroundColor: Colors.cyan,);
  }
}
